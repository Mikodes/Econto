name: Server

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: build
      run: cd server && npm install
      
    - name: lint
      run: cd server && npm run lint
